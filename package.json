{
  "name": "maze-game",
  "private": true,
  "version": "0.9.0",
  "type": "module",
  "description": "SceneryStack port of the PhET 'Maze Game' simulation. Drive a particle through tile-based mazes using position, velocity, or acceleration control modes.",
  "license": "AGPL-3.0-or-later",
  "repository": {
    "type": "git",
    "url": "https://github.com/OpenPhysics/MazeGame.git"
  },
  "keywords": [
    "maze-game",
    "simulation",
    "SceneryStack",
    "interactive",
    "physics",
    "kinematics",
    "vectors",
    "education",
    "pwa"
  ],
  "scripts": {
    "start": "vite",
    "dev": "vite",
    "build": "tsc && vite build",
    "build:single": "tsc && vite build --mode single",
    "preview": "vite preview",
    "lint": "biome check .",
    "format": "biome format --write .",
    "fix": "biome check --write .",
    "check": "tsc --noEmit && tsc -p tsconfig.scripts.json --noEmit && tsc -p tsconfig.test.json --noEmit",
    "release": "npm run check && npm run lint && npm run build && npm version patch && git push && git push --tags",
    "watch": "tsc --watch",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:query-params": "node scripts/check-query-params.mjs",
    "icons": "tsx scripts/generate-icons.ts",
    "clean": "rm -rf dist",
    "postinstall": "node scripts/ensure-rolldown-binding.mjs",
    "prepare": "git rev-parse --is-inside-work-tree >/dev/null 2>&1 && git config core.hooksPath .githooks || true"
  },
  "dependencies": {
    "scenerystack": "^3.0.0"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.0",
    "@rolldown/binding-linux-x64-gnu": "^1.1.2",
    "@types/node": "^24.13.2",
    "happy-dom": "^20.10.6",
    "playwright": "^1.61.0",
    "png-to-ico": "^3.0.1",
    "sharp": "^0.35.2",
    "tsx": "^4.22.4",
    "typescript": "^6.0.3",
    "vite": "^8.0.16",
    "vite-plugin-pwa": "^1.3.0",
    "vitest": "^4.1.9"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "overrides": {
    "lodash": "^4.18.0",
    "three": "^0.125.0"
  }
}
